// Demo xử lý nút "Lập phiếu nhanh"
document.getElementById("quickSlip")?.addEventListener("click", function() {
  alert("Chức năng lập phiếu nhanh đang được phát triển!");
});

// Sau này bạn có thể fetch dữ liệu từ backend để cập nhật số liệu
// Ví dụ:
// fetch('/api/dashboard')
//   .then(res => res.json())
//   .then(data => {
//     document.getElementById("tonkho").textContent = data.tonkho;
//     document.getElementById("phieu").textContent = data.phieu;
//     document.getElementById("trongkho").textContent = data.trongkho;
//     document.getElementById("docgia").textContent = data.docgia;
//   });
