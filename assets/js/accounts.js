// accounts.js
// Logic quản lý tài khoản được xử lý inline trong taikhoan.html
// File này giữ lại để tương thích cấu trúc project

// TODO: Khi có backend, thay thế bằng các API call:
//
// GET    /api/accounts          → lấy danh sách tài khoản
// POST   /api/accounts          → tạo tài khoản mới
// PUT    /api/accounts/:id      → cập nhật tài khoản
// DELETE /api/accounts/:id      → xóa tài khoản
//
// Ví dụ:
// async function fetchAccounts() {
//   const res = await fetch('/api/accounts', {
//     headers: { 'Authorization': `Bearer ${localStorage.getItem('token')}` }
//   });
//   return await res.json();
// }
